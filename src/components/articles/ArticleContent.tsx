import React from 'react';

interface Props {
    title: string;
    description: string;
}

const ArticleContent: React.FC = () => {
    return (
        <div>
            
        </div>
    );
};

export default ArticleContent;