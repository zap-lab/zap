import React from 'react';

interface Props {
    description: string;
}

const Description: React.FC = () => {
    return (
        <div>
            
        </div>
    );
};

export default Description;